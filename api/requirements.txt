# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.35
alembic==1.13.3

# Auth — Supabase JWT verification (JWKS + ES256/HS256)
PyJWT[crypto]==2.9.0
python-multipart==0.0.12

# Redis + job queue
redis==5.0.8
rq==1.16.2
rq-scheduler==0.13.1

# Media storage (S3-compatible — works with Cloudflare R2)
boto3==1.35.20

# Push notifications
exponent-server-sdk==2.1.0

# HTTP client (for Supabase admin calls)
httpx==0.27.2

# Config / env
pydantic==2.9.2
pydantic-settings==2.5.2

# Content moderation (basic profanity/keyword filter for MVP)
better-profanity==0.7.0

# Utilities
python-dateutil==2.9.0

# Dev / test only
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
